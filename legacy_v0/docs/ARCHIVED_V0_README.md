# Archived V0 Documents

This directory is reserved for V0 historical documents.

Do not read archived V0 documents by default during V1 work.

Use archived V0 documents only when:

- migrating a V0 module;
- explaining old behavior;
- comparing V0 and V1 design decisions;
- auditing historical implementation evidence.

Currently archived:

- `PROJECT_CONTEXT_legacy.md`
- `LEARNING_PROGRESS_legacy.md`
- `entrypoints/main_legacy.py`
- `entrypoints/log_viewer_legacy.py`
- `entrypoints/product_ui_legacy.py`

Old `plans/*.md` files are still in the legacy `plans/` directory until a later archive step.
