# Legacy V0 Logs

This directory is reserved for V0 runtime log artifacts.

The original root `logs/` directory contained local runtime output and was not preserved as stable source material. Keep future V1 logs outside this archive unless they are intentionally captured as a small, reviewed fixture or report.
