# Domain 文档

本仓库使用单一上下文的 domain 文档布局。

在探索 domain 或架构工作前，engineering skills 应先读取根目录的 `CONTEXT.md`（如果存在），再读取 `docs/adr/` 下相关 ADR（如果存在）。

如果这些文件不存在，静默继续。除非当前任务正在澄清应记录的 domain 术语或架构决策，否则不要提前创建这些文件。

当 `CONTEXT.md` 存在时，使用其中的词汇。如果输出会与现有 ADR 冲突，应明确指出该冲突。
